# Normally generated by roxygen2::document() / devtools::document() -- do
# not hand-edit in a real package. Written manually here because this
# scaffold isn't run through roxygen2 before distribution to students; see
# notes.md. Running devtools::document() from this folder will regenerate
# it safely (and won't change its contents, since it matches the roxygen2
# @export tag in R/stats.R already).
export(mean_by_site)
